# Planetary theme packs are ZIP containers with a custom application
# extension. Keep the extension intact when browsers download them.
AddType application/octet-stream .planetarytheme

<IfModule mod_headers.c>
    Header set Content-Disposition "attachment"
</IfModule>
